## 🌻 向日葵Git仓库管理 v1.1.7

### ✨ 文件变更

- **区分变更类型图标**：「📝 文件变更」列表中按类型显示：➕ 新增、✏️ 修改、🗑️ 删除、🔄 重命名，不再全部显示 📄。
- **底层统一**：新增 `getFileChangeType(file)`，对 `index`/`working_dir` 做 trim 后按优先级判断类型，与主进程提交摘要逻辑一致；图标悬停显示「新增/修改/删除/重命名」；路径输出做 `escapeHtml` 防止 XSS。

### 📝 说明

- 版本号升级为 1.1.7
