## 🌻 向日葵Git仓库管理 v1.2.6

### ✨ 界面

- **统一设计令牌**：圆角、间距、按钮、焦点环、弹窗宽度改为 CSS 变量，减少硬编码冲突。
- **弹窗体系**：普通 / 大号（平台配置）/ 宽屏填满（同步配置）三级尺寸，去掉重复覆盖样式。
- **同步配置**：列表占满可用高度；从仓库与同步组信息密度提高。
- **平台 / 批量**：去掉内联样式；批量仓库列表改用统一 `check-list` 组件样式。
- **文档截图**：`docs/screenshots/` 收录主界面、平台配置、同步配置预览图，并完善 README。

### 🐛 / 维护

- 合并 `.modal-*` / `.form-*` / `.btn-primary` 重复定义，避免后段 CSS 覆盖前段导致视觉漂移。

### 📝 说明

- 版本号升级为 **1.2.6**。
