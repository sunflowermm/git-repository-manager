## 向日葵Git仓库管理 v1.2.9

### 更新检查代理

- **修复**：原先只把代理设到界面 `defaultSession`，而 `electron-updater` 使用独立分区会话，导致填了 7890 仍超时、必须开系统代理才通。
- 现将代理同时应用到 **updater session** + 主会话；并设置进程内 `HTTP(S)_PROXY` 供附属请求。
- 尝试顺序：本地代理端口 → 系统代理 → 直连；端口未监听则跳过并提示。
- 单次检查超时改为 20s，失败更快换路。

### 说明

- 版本号升级为 **1.2.9**。
