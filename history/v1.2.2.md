## 🌻 向日葵Git仓库管理 v1.2.2

### 🐛 修复

- **更新弹窗不显示**：UI 精修误将 `.modal-overlay` 设为 `position: relative`，在 `body { overflow: hidden }` 下弹窗被挤出屏幕；已恢复为 `fixed` 高层级。
- **Gitee 自动更新缺安装包**：`latest` 标签附件堆积导致配额满，新 exe 上传失败；发布前自动清理旧安装包再上传。

### 📝 说明

- 若当前仍卡在 1.2.0/1.2.1 且看不到更新按钮，请先到发行页手动安装本版本，之后检查更新弹窗会正常出现。
- 版本号升级为 **1.2.2**。
