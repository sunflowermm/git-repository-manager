## 🌻 向日葵Git仓库管理 v1.2.1

### 🐛 紧急修复

- **修复「An object could not be cloned」**：Vue 响应式 Proxy 无法经 Electron IPC 传递，导致刷新仓库列表失败、界面看起来像「仓库全没了」。
- **配置未丢失**：本地 `config.json` 中的仓库路径仍在；修复后重新打开即可恢复列表。
- IPC 发送前统一 JSON 序列化；主进程返回的文件状态改为纯对象，避免克隆失败。

### 📝 说明

- 版本号升级为 **1.2.1**。
