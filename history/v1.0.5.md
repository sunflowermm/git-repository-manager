# v1.0.5 更新日志

## ✨ 新功能
- 无

## 🔧 优化
- **CSS 样式完善**：新增 `--primary-color` 与主题一致，浅色/深色主题下更新弹窗颜色统一
- **更新弹窗视觉统一**：
  - 新增 `.update-banner` / `.update-banner--success`：发现新版本与更新已就绪的顶部横幅，统一圆角、间距与渐变
  - 新增 `.update-notes-wrap`：更新说明区域统一最大高度、内边距与边框
  - 新增 `.update-dialog-desc`：弹窗底部说明文字统一样式
  - 新增 `.update-progress-track` / `.update-progress-bar` / `.update-progress-text`：下载进度条与文案统一样式
- **渲染层**：更新弹窗改为使用上述 CSS 类，去除内联样式，便于维护与主题切换

## 🐛 修复
- 无

## 📝 说明
- 更新流程与交互与 v1.0.4 一致，仅视觉与样式代码结构优化，版本号升级为 1.0.5。
