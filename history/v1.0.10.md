# v1.0.10 更新日志

## 🔧 修复与优化

### 拖拽排序修复
- **路径排序**：改为按 `repoPaths` 中的路径重排，解决 `repos` 与 `repoPaths` 索引不一致导致的排序失败
- **自定义拖拽**：使用 mousedown/mousemove/mouseup 替代 HTML5 DnD，规避 Electron 下原生拖拽失效

## 🔧 优化
- 选中卡片 hover 保持主题色，不再变白
- 搜索时隐藏拖拽手柄
